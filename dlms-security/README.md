# dlms-security

Security layer for DLMS/COSEM.

## Overview

- Security Suite 0–5 support
- HLS-ISM (High Level Security) authentication
- AES-128-GCM encryption/decryption (behind `aes` feature)

## Dependencies

- `dlms-core`
- `aes-gcm` (optional, behind `aes` feature)
